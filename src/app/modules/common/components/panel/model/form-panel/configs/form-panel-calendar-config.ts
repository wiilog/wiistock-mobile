import {FormPanelCalendarMode} from '@common/components/panel/form-panel/form-panel-calendar/form-panel-calendar-mode';

export interface FormPanelCalendarConfig {
    required?: boolean;
    mode: FormPanelCalendarMode;
}
